/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.eusemfronteiras.com.br', 't1.ea.ltmcdn.com', 'cptstatic.s3.amazonaws.com']
  }
}

module.exports = nextConfig
